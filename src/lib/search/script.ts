/**
 * 검색 쿼리·문서의 스크립트 판별 — W7 (F-07-02)
 *
 * 정본: 07-search-navigation.md F-07-02 엣지 케이스
 *   *"매우 짧은 쿼리(1~2자): CJK에서는 유효 쿼리다. 헬프센터가 'You can use
 *   queries with CJK characters in search!' 로 CJK를 명시 지원하므로, **최소 길이
 *   제한을 영문 기준(3자 등)으로 두면 한국어에서 검색이 통째로 죽는다.** 최소
 *   길이는 스크립트 판별 후 CJK는 1자, 라틴은 2자로 분기할 것."*
 *
 * ──────────────────────────────────────────────────────────────────────
 * 이 판별이 두 가지를 동시에 결정한다
 * ──────────────────────────────────────────────────────────────────────
 *
 * 1. **최소 쿼리 길이** — CJK 1자, 라틴 2자 (위 인용)
 * 2. **검색 축** — CJK 는 pg_bigm(2-gram), 라틴은 tsvector
 *
 * 축이 둘인 이유는 마이그레이션 0012 머리말에 실측과 함께 적혀 있다. 요약하면
 * PostgreSQL 에 한국어 text search config 가 없어서 `'검색'` 쿼리가
 * `'검색이 빠르다'` 를 찾지 못한다. pg_bigm 은 찾는다.
 *
 * 같은 함수가 색인 쪽 `lang` 값도 만든다. 한 규칙이 양쪽을 지배하게 두면
 * "색인은 한국어로 판단했는데 쿼리는 라틴으로 판단하는" 상태가 생기지 않는다.
 */

/**
 * CJK 범위.
 *
 * 한글(음절·자모), 한자(통합·호환), 일본어(히라가나·가타카나)를 함께 본다.
 * pg_bigm 의 2-gram 이 이 문자들 전부에 대해 같은 이유로 필요하다 — 공백으로
 * 단어 경계가 드러나지 않는다.
 *
 * ⚠ 한글 호환 자모(`㄰-㆏`)를 빼면 **IME 조합 중간 상태가 라틴으로
 * 판정된다.** 사용자가 "ㄱ" 만 친 순간 최소 길이 2자 규칙에 걸려 검색이 안 되고,
 * 한 글자를 더 쳐서 "가" 가 되는 순간 갑자기 동작한다 — 쓰는 쪽에서는 버그로 보인다.
 */
const CJK =
  /[ᄀ-ᇿ぀-ヿ㄰-㆏㐀-䶿一-鿿ꥠ-꥿가-퟿豈-﫿ｦ-ﾟ]/u

export type Script = 'cjk' | 'latin'

/** 감지 결과를 `search_document.lang` 에 넣을 때 쓰는 태그. */
export type LangTag = 'ko' | 'en' | 'und'

/**
 * 이 문자열을 어느 축으로 다룰 것인가.
 *
 * **CJK 문자가 하나라도 있으면 `'cjk'` 다.** 비율로 판단하지 않는다 —
 * `"Notion 클론"` 처럼 섞인 쿼리에서 라틴을 고르면 한국어 부분이 조사 문제로
 * 통째로 빠진다. bigm 은 라틴도 부분 문자열로 찾으므로(단어 경계를 모를 뿐)
 * 섞인 쿼리에서 bigm 을 고르는 쪽이 손실이 없다.
 */
export function detectScript(text: string): Script {
  return CJK.test(text) ? 'cjk' : 'latin'
}

/** 스크립트별 최소 쿼리 길이. 이보다 짧으면 질의하지 않는다. */
export function minQueryLength(script: Script): number {
  return script === 'cjk' ? 1 : 2
}

/**
 * 색인 문서의 `lang`.
 *
 * 정교한 언어 감지가 아니다 — 한글이 있으면 `'ko'`, 라틴 글자가 있으면 `'en'`,
 * 둘 다 없으면 `'und'`. **Phase 0 의 쿼리 경로는 이 값을 읽지 않는다**(분기는
 * 쿼리 쪽 스크립트로 하고 regconfig 는 `simple` 고정이다). F-07-06 이 "언어 감지
 * 결과를 문서에 넣어" 를 요구해 채우고, 지금은 진단용이다.
 *
 * 한자·가나만 있는 문서도 `'ko'` 가 아니다 — 그 경우 `'und'` 로 둔다. 일본어·
 * 중국어 판별은 이 함수가 할 수 있는 일이 아니고, 못 하는 일을 한 척하지 않는다.
 */
export function detectLang(text: string): LangTag {
  if (/[ᄀ-ᇿ㄰-㆏ꥠ-꥿가-퟿]/u.test(text)) return 'ko'
  if (/[A-Za-z]/.test(text)) return 'en'
  return 'und'
}
