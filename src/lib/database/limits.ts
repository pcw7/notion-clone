/**
 * 행 질의의 크기 상수 — 서버(`query.ts`)와 화면(표)이 **같은 값**을 본다.
 *
 * `query.ts` 는 DB 커넥션을 끌고 오므로 클라이언트 번들에 들어갈 수 없다. 그런데
 * 화면도 상한을 알아야 한다 — "더 보기"가 언제 멈춰야 하는지가 이 값이다.
 * F-03-17: *"누적 페이지네이션 깊이가 10,000건으로 제한되며 … '전부 훑는'
 * 클라이언트 로직은 반드시 깨진다."* 화면이 같은 숫자를 따로 적어 두면 둘이
 * 갈라지는 날이 온다.
 */

/** 정본 §3.5 DB 상수. 이 깊이를 넘으면 완주를 보장하지 않는다. */
export const MAX_QUERY_PAGINATION = 10_000
/** 마스터 문서 W8-b: "keyset 커서 + '더 보기' 50행". */
export const DEFAULT_QUERY_LIMIT = 50
export const MAX_QUERY_LIMIT = 100
